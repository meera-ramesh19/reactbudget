import ShowTransactions from '../Components/ShowTransactions';

const Show = () => {
  return (
    <div 
      style={{textAlign:'center'}}>
      <ShowTransactions />
    </div>
  );
};

export default Show;
